class User {
    constructor(userId, userName, userEmail, userRole){
        this.userId = userId;
        this.userName = userName;
        this.userEmail = userEmail;
        this.userRole = userRole;
    };
};

export default User;